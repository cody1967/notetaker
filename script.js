const noteTitle = document.getElementById("title")
const noteText = document.getElementById("text")
const noteSave = document.getElementById("save")
const saveNotes = document.getElementById("savenotes")
// use the const to connect user input from html to script file
function myFunction() {
  document.getElementById("text").innerHTML = saveNotes;
// note save button click to connect to server.js api
object.addEventListener("click", noteSave);
// save notes connects to get api/notes.append Notes to this const . for loop to append
}

for (let i = 0; i < array.length; i++) {
  const saveNotes = array[i];
  
}